# Publication provenance

This is a multi-repository workspace. It has no independent LC semantic release or `lc-export.json`; those identities and receipts belong to its two pinned children. `workspace.json` records their semantic releases and publication commits. `publication-files.json` hashes workspace files and records the exact child gitlinks. The declared WeightedSieve assumption remains unchanged.
